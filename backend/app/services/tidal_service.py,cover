> import logging

> from app.services.base_music_service import BaseMusicService

> logger = logging.getLogger(__name__)


> class TidalService(BaseMusicService):
>     def __init__(self):
>         super().__init__()
>         self.source_name = "tidal"

>     def can_handle(self, url: str) -> bool:
!         return "tidal.com" in url


> tidal_service = TidalService()
